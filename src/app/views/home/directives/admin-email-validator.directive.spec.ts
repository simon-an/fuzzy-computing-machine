import { AdminEmailValidatorDirective } from './admin-email-validator.directive';

describe('AdminEmailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminEmailValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
